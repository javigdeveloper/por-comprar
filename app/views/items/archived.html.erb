<h1 class="text-2xl font-bold mb-4">📦 Archivados</h1>

<% if @items.any? %>
  <ul class="space-y-2">
    <% @items.each do |item| %>
      <li class="bg-gray-100 p-2 rounded"><%= item.name %></li>
    <% end %>
  </ul>
<% else %>
  <p class="text-gray-500">No hay artículos archivados.</p>
<% end %>
