<h1 class="text-2xl font-bold mb-4">🌟 Artículos Populares</h1>

<% if @popular_items.any? %>
  <ul class="space-y-2">
    <% @popular_items.each do |item_name| %>
      <li id="popular_item_<%= item_name.parameterize %>" class="bg-yellow-100 p-2 rounded flex justify-between items-center">
        <span><%= item_name %></span>

        <%= button_to "Agregar", create_from_popular_items_path(name: item_name),
            method: :post,
            data: { turbo_stream: true },
            class: "text-sm text-white bg-green-500 hover:bg-green-600 px-3 py-1 rounded" %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p class="text-gray-500">No hay artículos populares por ahora.</p>
<% end %>
